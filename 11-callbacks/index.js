/*
Callbacks: Callbacks are functions passed as arguments to another function to be executed later
*/

function fetchData(callback) {
    // Fetch data from API
    callback(data);
}

// const fetchData = (callback) => {
//     // Fetch data from API
//     callback(data)
// }