/*
Functions: Functions are blocks of reusable code that perform a specific task and can take 
parameters and return values. 
*/

function addNumbers(a, b) {
    return a + b;
}
console.log(addNumbers(4, 16));

// The name of the above function is 'addNumbers'